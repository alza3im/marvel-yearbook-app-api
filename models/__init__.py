# import uuid

# from flask_sqlalchemy import SQLAlchemy

# db = SQLAlchemy()


# class Character(db.Model):
#     id = db.Column(db.String(), primary_key=True, default=lambda: str(uuid.uuid4()))
#     name = db.Column(db.String(), uniqu=True)
#     description = db.Column(db.String())
